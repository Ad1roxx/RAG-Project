# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Generation LLM (Sprint 4) — Google AI Studio free tier
GEMINI_API_KEY=

# Evaluation LLM-as-judge (Sprint 5) — Groq free tier
GROQ_API_KEY=
